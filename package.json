{
  "name": "jj-llm-switch",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "bin": {
    "jj-llm-switch": "./src/cli.ts"
  },
  "scripts": {
    "build": "mkdir -p dist && bun run build.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  }
}
