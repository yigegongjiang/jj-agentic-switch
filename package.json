{
  "name": "jj-agentic-switch",
  "version": "0.5.3",
  "private": true,
  "type": "module",
  "bin": {
    "jj-agentic-switch": "./src/cli.ts"
  },
  "scripts": {
    "build": "mkdir -p dist && bun run build.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  }
}
